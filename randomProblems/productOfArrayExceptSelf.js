// 
function productOfArrayExceptSelf(nums) {
  const output = []

  nums.forEach(function(num) {
    console.log(num)
  })

  // interate through the right, setting output to the final 

  return output
}

// tests
const listOne = [1,2,3,4]
productOfArrayExceptSelf(listOne)
