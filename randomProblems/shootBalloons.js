function shootBalloons(balloons) {
  // helper data structure


  // iterate through balloons - balloon
    // at balloon, we ask:
      // does this overlap with an existing balloon / get popped by an existing arrow
      // if so:
        // don't add arrow count
      // if not:
        // add new arrow
      // remember this balloon
}

var testOne = [ [1,2], [2, 6], [3, 4], [2, 7] ] 
console.log(shootBalloons(testOne))
