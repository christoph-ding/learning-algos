function bubbleSort(input) {
  // no output because we bubbleSort in place

  // iterate through input,
    // compare and if applicable,

    // swap

}

// tests
var testOne = [1, 21, 30, 0];
var testTwo = [10, 8, 2, 6, 3];
