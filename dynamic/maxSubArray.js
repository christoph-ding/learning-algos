function maxSubArray(numberList, k) {
  // what if k is larger than number of items in list?

  // we use a helper queue

  // iterate through the numberList
    // for each, apply the comparison


  // go through the helper queue and find the max

}

// tests
var listOne = [1,2,3,4,5,6,7,8,9];
var listTwo = [-10, 23, 100, -30, 24, 93, 1, -9, 23, -4, 11];
